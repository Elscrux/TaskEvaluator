namespace Task;

public class TaskClass {
    /// <summary>
    /// Input to this function is a string containing multiple groups of nested parentheses.
    /// Your goal is to separate those group into separate strings and return the list of those.
    /// Separate groups are balanced (each open brace is properly closed) and not nested within each other.
    /// Ignore any spaces in the input string.
    /// </summary>
    /// <param name="parenthesesString">String containing multiple groups of nested parentheses</param>
    /// <returns>List of strings containing separate groups of nested parentheses</returns>
    public static IReadOnlyList<string> SeparateParenthesisGroups(string parenthesesString) { 
        INSERT_CODE_HERE
    }
}
