namespace Task;

public class TaskClass {
    /// <summary>
    /// Check if in given list of numbers, are any two numbers closer to each other than the given threshold.
    /// </summary>
    /// <param name="numbers">List of floats</param>
    /// <param name="treshold">Threshold to compare numbers with</param>
    /// <returns>true, if there are any two numbers closer to each other than the given threshold</returns>
    public static bool HasCloseElements(IReadOnlyList<float> numbers, float treshold) {
        INSERT_CODE_HERE
    }
}
